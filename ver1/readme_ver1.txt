ver1은 기존에 외부 (최상위)디렉터리에 잔류하던 파일들을 끌어왔습니다.
이는 프로젝트의 결과물(서버와 클라이언트의 파일) 최상(위)단 디렉토리에 위치시키기 위해 결정했습니다.

ver1 dragged files that were previously left in the external (top-level) directory.
This was determined to be located in the top (top) directory of the project's output (the files of servers and clients).